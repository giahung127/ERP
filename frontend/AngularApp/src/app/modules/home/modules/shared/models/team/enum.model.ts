export enum TeamStatus {
    Active = 'Active',
    Disabled = 'Disabled'
}

export enum EmployeeStatus {
    Active = 'Active',
    Terminated = 'Terminated'
}

export enum Gender {
    Male = 'Male',
    Female = 'Female'
}
